import React from 'react';

const Check = (props) => {
    const {paymentData} =props;
    console.log(paymentData);

    return (
        <div>
            
        </div>
    );
};

export default Check;